# Core Flask and web server
flask>=2.3.0
gunicorn>=20.1.0

# TensorFlow Lite for AI inference
tensorflow>=2.13.0

# Image processing
Pillow>=10.0.0

# HTTP requests for ThingsBoard integration
requests>=2.31.0

# Scientific computing
numpy>=1.24.0

# Base64 encoding/decoding (usually built-in but explicit is better)
pybase64>=1.3.0
